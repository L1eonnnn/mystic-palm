# OPENROUTER_API_KEY: 核心密钥（必需）。通过 OpenRouter 统一掌管和调用所有大模型，包括 Gemini 2.5 Flash, Gemini 2.5 Pro, GPT-4o, GPT-4o Mini 等。
OPENROUTER_API_KEY=

# VITE_DASHSCOPE_API_KEY: Required for Aliyun DashScope (Qwen-VL-Max) API calls.
VITE_DASHSCOPE_API_KEY="YOUR_ALIYUN_API_KEY"

# VITE_DEEPSEEK_API_KEY: Required for DeepSeek API calls.
VITE_DEEPSEEK_API_KEY="YOUR_DEEPSEEK_API_KEY"

# VITE_ZHIPU_API_KEY: Required for Zhipu (GLM-4V) API calls.
VITE_ZHIPU_API_KEY="YOUR_ZHIPU_API_KEY"

# APP_URL: The URL where this applet is hosted.
# AI Studio automatically injects this at runtime with the Cloud Run service URL.
# Used for self-referential links, OAuth callbacks, and API endpoints.
APP_URL="MY_APP_URL"

# SMTP Mail Settings (Optional, configures real email dispatch for logins)
# To use real email sending, input your SMTP credentials in the AI Studio Settings secrets panel
SMTP_HOST="smtp.gmail.com"
SMTP_PORT=465
SMTP_USER=
SMTP_PASS=
SMTP_FROM_NAME="MysticPalm 占星殿"

